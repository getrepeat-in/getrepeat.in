import CartPageUI from "@/components/pages/cart";
export const metadata = {
    title: "Cart | GetRepeat",
    description: "Review and complete your subscription",
};

export default function CartPage() {
    return <CartPageUI />;
}